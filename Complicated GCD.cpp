#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    string s,s1;
    cin>>s>>s1;

    int n=s.size(), n1=s1.size();
    if(s==s1)
    cout<<s<<endl;
    else 
    cout<<1<<endl;

}